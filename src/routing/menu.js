import { ContainerOutlined } from '@ant-design/icons';
import { POSTS_PATH } from './paths';

export default {
  public: [
    {
      title: 'Posts',
      path: POSTS_PATH,
      icon: ContainerOutlined,
    },
  ],
};
